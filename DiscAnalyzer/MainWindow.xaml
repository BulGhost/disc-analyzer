<RibbonWindow x:Class="DiscAnalyzer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DiscAnalyzer"
        xmlns:tree="clr-namespace:Aga.Controls.Tree;assembly=Aga.Controls"
        xmlns:converters="clr-namespace:DiscAnalyzer.HelperClasses.Converters"
        mc:Ignorable="d"
        Title="Disc Analyzer"  Height="550" Width="900" Icon="Icon.ico">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Ribbon Grid.Row="0">
            <Ribbon.ApplicationMenu>
                <RibbonApplicationMenu KeyTip="F">
                    <RibbonApplicationMenu.SmallImageSource>
                        <DrawingImage>
                            <DrawingImage.Drawing>
                                <GeometryDrawing>
                                    <GeometryDrawing.Geometry>
                                        <RectangleGeometry Rect="0,0,20,20"></RectangleGeometry>
                                    </GeometryDrawing.Geometry>
                                    <GeometryDrawing.Brush>
                                        <VisualBrush  Stretch="Uniform">
                                            <VisualBrush.Visual>
                                                <TextBlock Text="File" FontSize="16" Foreground="White"/>
                                            </VisualBrush.Visual>
                                        </VisualBrush>
                                    </GeometryDrawing.Brush>
                                </GeometryDrawing>
                            </DrawingImage.Drawing>
                        </DrawingImage>
                    </RibbonApplicationMenu.SmallImageSource>
                    <RibbonApplicationMenuItem Header="Select Directory" ImageSource="\Images/MenuIcons/open-64.png"
                                               Command="{Binding Path=OpenDialogCommand}" />
                    <RibbonApplicationMenuItem Header="Refresh" ImageSource="\Images/MenuIcons/refresh-64.png"
                                               Command="{Binding Path=RefreshCommand}"/>
                    <RibbonApplicationMenuItem Header="Stop Scan" ImageSource="\Images/MenuIcons/stop-64.png"
                                               Command="{Binding Path=StopCommand}"/>
                    <RibbonApplicationMenuItem Header="Exit" ImageSource="\Images/MenuIcons/exit-64.png"
                                               Command="{Binding Path=ExitCommand}"/>
                </RibbonApplicationMenu>
            </Ribbon.ApplicationMenu>
            <RibbonTab Header="Home" KeyTip="H">
                <RibbonGroup Header="Scan">
                    <RibbonSplitButton  Label="Select Directory" Command="{Binding Path=OpenDialogCommand}"
                                        LargeImageSource="/Images/MenuIcons/open-64.png"
                                        SmallImageSource="/Images/MenuIcons/open-32.png"
                                        ItemsSource="{Binding SelectDirectoryMenuItems}">
                        <RibbonSplitButton.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" Padding="3">
                                            <TextBlock.Background>
                                                <LinearGradientBrush EndPoint="1,0" StartPoint="0,0">
                                                    <GradientStop Color="#FFECF0F6" Offset="0"/>
                                                    <GradientStop Color="#FFDEE4EA" Offset="1"/>
                                                </LinearGradientBrush>
                                            </TextBlock.Background>
                                        </TextBlock>
                                    </DataTemplate>
                                </GroupStyle.HeaderTemplate>
                            </GroupStyle>
                        </RibbonSplitButton.GroupStyle>
                        <RibbonSplitButton.ItemTemplate>
                            <DataTemplate>
                                <Border>
                                    <Border.InputBindings>
                                            <MouseBinding MouseAction="LeftClick" Command="{Binding Command}"/>
                                    </Border.InputBindings>
                                    <TextBlock Text="{Binding Name}"/>
                                </Border>
                            </DataTemplate>
                        </RibbonSplitButton.ItemTemplate>
                        <RibbonSplitButton.ItemContainerStyle>
                            <Style TargetType="RibbonMenuItem">
                                <Setter Property="ImageSource" Value="{Binding Category,
                                    Converter={x:Static converters:CategoryToImageConverter.Instance}}"></Setter>
                                <Setter Property="Margin" Value="-6,0,0,0"></Setter>
                                <Setter Property="Padding" Value="5"></Setter>
                            </Style>
                        </RibbonSplitButton.ItemContainerStyle>
                    </RibbonSplitButton>
                    <RibbonButton Label="Stop Scan"
                                  LargeImageSource="/Images/MenuIcons/stop-64.png"
                                  SmallImageSource="/Images/MenuIcons/stop-32.png"
                                  Command="{Binding Path=StopCommand}"/>
                    <RibbonButton Label="Refresh"
                                  LargeImageSource="/Images/MenuIcons/refresh-64.png"
                                  SmallImageSource="/Images/MenuIcons/refresh-32.png"
                                  Command="{Binding Path=RefreshCommand}"/>
                </RibbonGroup>
                <RibbonGroup Header="Mode">
                    <RibbonRadioButton Label="Size"
                                  LargeImageSource="/Images/MenuIcons/size-64.png"
                                  SmallImageSource="/Images/MenuIcons/size-32.png"
                                  IsChecked="{Binding Path=Mode,
                        Converter={x:Static converters:ModeConverter.Instance},
                        ConverterParameter={x:Static local:ItemProperty.Size}}"/>
                    <RibbonRadioButton Label="Allocated Space"
                                  LargeImageSource="/Images/MenuIcons/space-64.png"
                                  SmallImageSource="/Images/MenuIcons/space-32.png"
                                  IsChecked="{Binding Path=Mode,
                        Converter={x:Static converters:ModeConverter.Instance},
                        ConverterParameter={x:Static local:ItemProperty.Allocated}}"/>
                    <RibbonRadioButton Label="File Count"
                                  LargeImageSource="/Images/MenuIcons/count-64.png"
                                  SmallImageSource="/Images/MenuIcons/count-32.png"
                                  IsChecked="{Binding Path=Mode,
                        Converter={x:Static converters:ModeConverter.Instance},
                        ConverterParameter={x:Static local:ItemProperty.Files}}"/>
                    <RibbonRadioButton Label="Percent"
                                  LargeImageSource="/Images/MenuIcons/percent-64.png"
                                  SmallImageSource="/Images/MenuIcons/percent-32.png"
                                  IsChecked="{Binding Path=Mode,
                        Converter={x:Static converters:ModeConverter.Instance},
                        ConverterParameter={x:Static local:ItemProperty.PercentOfParent}}"/>
                </RibbonGroup>
                <RibbonGroup Header="Unit">
                    <RibbonRadioButton Label="Automatic Units"
                                  LargeImageSource="/Images/MenuIcons/auto-64.png"
                                  SmallImageSource="/Images/MenuIcons/auto-32.png"
                                  IsChecked="{Binding Path=Unit,
                        Converter={x:Static converters:UnitConverter.Instance},
                        ConverterParameter={x:Static local:Unit.Auto}}"/>
                    <RibbonRadioButton SmallImageSource="/Images/MenuIcons/gb-32.png"
                                       IsChecked="{Binding Path=Unit,
                        Converter={x:Static converters:UnitConverter.Instance},
                        ConverterParameter={x:Static local:Unit.Gb}}"/>
                    <RibbonRadioButton SmallImageSource="/Images/MenuIcons/mb-32.png"
                                       IsChecked="{Binding Path=Unit,
                        Converter={x:Static converters:UnitConverter.Instance},
                        ConverterParameter={x:Static local:Unit.Mb}}"/>
                    <RibbonRadioButton SmallImageSource="/Images/MenuIcons/kb-32.png"
                                       IsChecked="{Binding Path=Unit,
                        Converter={x:Static converters:UnitConverter.Instance},
                        ConverterParameter={x:Static local:Unit.Kb}}"/>
                </RibbonGroup>
                <RibbonGroup Header="Expand">
                    <RibbonMenuButton Label="Expand"
                                  LargeImageSource="/Images/MenuIcons/expand-64.png"
                                  SmallImageSource="/Images/MenuIcons/expand-32.png">
                        <RibbonMenuItem Header="To Level 1" Command="{Binding ExpandCommand}"
                                        CommandParameter="{x:Static local:ExpandLevel.Level1}"/>
                        <RibbonMenuItem Header="To Level 2" Command="{Binding ExpandCommand}"
                                        CommandParameter="{x:Static local:ExpandLevel.Level2}"/>
                        <RibbonMenuItem Header="To Level 3" Command="{Binding ExpandCommand}"
                                        CommandParameter="{x:Static local:ExpandLevel.Level3}"/>
                        <RibbonMenuItem Header="To Level 4" Command="{Binding ExpandCommand}"
                                        CommandParameter="{x:Static local:ExpandLevel.Level4}"/>
                        <RibbonMenuItem Header="To Level 5" Command="{Binding ExpandCommand}"
                                        CommandParameter="{x:Static local:ExpandLevel.Level5}"/>
                        <RibbonSeparator />
                        <RibbonMenuItem Header="Full Expand" ImageSource="/Images/MenuIcons/expand-32.png"
                                        Command="{Binding ExpandCommand}"
                                        CommandParameter="{x:Static local:ExpandLevel.FullExpand}"/>
                    </RibbonMenuButton>
                </RibbonGroup>
            </RibbonTab>
        </Ribbon>
        <Grid Grid.Row="1" >
            <tree:TreeList Name="Tree">
                <tree:TreeList.Resources>
                    <Style x:Key="LeftColAlign">
                        <Setter Property="GridViewColumnHeader.Padding" Value="7, 2, 20, 2"/>
                        <Setter Property="GridViewColumnHeader.HorizontalContentAlignment" Value="Left" />
                    </Style>
                    <Style x:Key="RightColAlign">
                        <Setter Property="GridViewColumnHeader.Padding" Value="2, 2, 20, 2"/>
                        <Setter Property="GridViewColumnHeader.HorizontalContentAlignment" Value="Right" />
                    </Style>
                    <Style x:Key="CenterColAlign">
                        <Setter Property="GridViewColumnHeader.Padding" Value="2, 2, 20, 2"/>
                        <Setter Property="GridViewColumnHeader.HorizontalContentAlignment" Value="Center" />
                    </Style>
                </tree:TreeList.Resources>
                <tree:TreeList.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </tree:TreeList.ItemContainerStyle>
                <tree:TreeList.View>
                    <GridView>
                        <GridView.Columns>
                            <GridViewColumn Width="300" HeaderContainerStyle="{StaticResource LeftColAlign}"
                                            Header="{Binding Path=NameColumnHeader}">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <tree:RowExpander />
                                            <Image Width="20" Source="{Binding Type,
                                            Converter={x:Static converters:HeaderToImageConverter.Instance}}">
                                            </Image>
                                            <TextBlock Name="Size" Padding="10, 0" VerticalAlignment="Center"
                                                       Visibility="{Binding Path=DataContext.Mode,
                                                        RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type RibbonWindow}},
                                                        Converter={x:Static converters:ModeToVisibilityConverter.Instance},
                                                        ConverterParameter={x:Static local:ItemProperty.Size}}" >
                                                <TextBlock.Text>
                                                    <MultiBinding Converter="{x:Static converters:ItemSizeConverter.Instance}">
                                                        <Binding Path="Size"/>
                                                        <Binding Path="DataContext.Unit"
                                                                 RelativeSource="{RelativeSource FindAncestor, AncestorType={x:Type RibbonWindow}}" />
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>
                                            <TextBlock Name="Allocated" Padding="10, 0" VerticalAlignment="Center"
                                                       Visibility="{Binding Path=DataContext.Mode,
                                                        RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type RibbonWindow}},
                                                        Converter={x:Static converters:ModeToVisibilityConverter.Instance},
                                                        ConverterParameter={x:Static local:ItemProperty.Allocated}}">
                                                <TextBlock.Text>
                                                    <MultiBinding Converter="{x:Static converters:ItemSizeConverter.Instance}">
                                                        <Binding Path="Allocated"/>
                                                        <Binding Path="DataContext.Unit"
                                                                 RelativeSource="{RelativeSource FindAncestor, AncestorType={x:Type RibbonWindow}}" />
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>
                                            <TextBlock Name="Files" Padding="10, 0" VerticalAlignment="Center"
                                                       Text="{Binding Files, StringFormat={}{0:N0} Files}"
                                                       Visibility="{Binding Path=DataContext.Mode,
                                                        RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type RibbonWindow}},
                                                        Converter={x:Static converters:ModeToVisibilityConverter.Instance},
                                                        ConverterParameter={x:Static local:ItemProperty.Files}}"/>
                                            <TextBlock Name="Percent" Padding="10, 0" VerticalAlignment="Center"
                                                       Text="{Binding PercentOfParent, Converter={x:Static converters:IntToPercentConverter.Instance}}"
                                                       Visibility="{Binding Path=DataContext.Mode,
                                                        RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type RibbonWindow}},
                                                        Converter={x:Static converters:ModeToVisibilityConverter.Instance},
                                                        ConverterParameter={x:Static local:ItemProperty.PercentOfParent}}"/>
                                            <TextBlock Name="Name" Text="{Binding Name}" VerticalAlignment="Center" />
                                        </StackPanel>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding IsLargeItem}" Value="True">
                                                <Setter TargetName="Size" Property="FontWeight" Value="Bold" />
                                                <Setter TargetName="Allocated" Property="FontWeight" Value="Bold" />
                                                <Setter TargetName="Files" Property="FontWeight" Value="Bold" />
                                                <Setter TargetName="Percent" Property="FontWeight" Value="Bold" />
                                                <Setter TargetName="Name" Property="FontWeight" Value="Bold" />
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn Width="85" HeaderContainerStyle="{StaticResource RightColAlign}"
                                            Header="{Binding Path=SizeColumnHeader}">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock x:Name="TextBlock123" HorizontalAlignment="Right">
                                            <TextBlock.Text>
                                                <MultiBinding Converter="{x:Static converters:ItemSizeConverter.Instance}">
                                                    <Binding Path="Size"/>
                                                    <Binding Path="DataContext.Unit"
                                                             RelativeSource="{RelativeSource FindAncestor, AncestorType={x:Type RibbonWindow}}" />
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding IsLargeItem}" Value="True">
                                                <!--<Setter TargetName="TextBlock" Property="FontWeight" Value="Bold" />-->
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn  Width="85" HeaderContainerStyle="{StaticResource RightColAlign}"
                                             Header="{Binding Path=AllocatedColumnHeader}">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Name="TextBlock" HorizontalAlignment="Right">
                                            <TextBlock.Text>
                                                <MultiBinding Converter="{x:Static converters:ItemSizeConverter.Instance}">
                                                    <Binding Path="Allocated"/>
                                                    <Binding Path="DataContext.Unit"
                                                             RelativeSource="{RelativeSource FindAncestor, AncestorType={x:Type RibbonWindow}}" />
                                                </MultiBinding>
                                            </TextBlock.Text>
                                        </TextBlock>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding IsLargeItem}" Value="True">
                                                <Setter TargetName="TextBlock" Property="FontWeight" Value="Bold" />
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn Width="70" HeaderContainerStyle="{StaticResource RightColAlign}"
                                            Header="{Binding Path=FilesColumnHeader}">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Name="TextBlock" HorizontalAlignment="Right" Text="{Binding Files,
                                                StringFormat={}{0:N0}}"/>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding IsLargeItem}" Value="True">
                                                <Setter TargetName="TextBlock" Property="FontWeight" Value="Bold" />
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn Width="70" HeaderContainerStyle="{StaticResource RightColAlign}"
                                            Header="{Binding Path=FoldersColumnHeader}">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Name="TextBlock" HorizontalAlignment="Right" Text="{Binding Folders,
                                                StringFormat={}{0:N0}}"/>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding IsLargeItem}" Value="True">
                                                <Setter TargetName="TextBlock" Property="FontWeight" Value="Bold" />
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn Width="150" HeaderContainerStyle="{StaticResource CenterColAlign}"
                                            Header="{Binding Path=PercentOfParentColumnHeader}">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel HorizontalAlignment="Stretch">
                                            <StackPanel.Background>
                                                <LinearGradientBrush EndPoint="1,0" StartPoint="0,0">
                                                    <GradientStop Color="#FFC0AADF" Offset="0"/>
                                                    <GradientStop x:Name="GradientStop" Color="#FF5F5DC5" Offset="{Binding PercentOfParent,
                                                        Converter={x:Static converters:PercentToOffsetConverter.Instance} }"/>
                                                    <GradientStop Color="Gainsboro" Offset="{Binding ElementName=GradientStop, Path=Offset}"/>
                                                    <GradientStop Color="Gainsboro" Offset="1"/>
                                                </LinearGradientBrush>
                                            </StackPanel.Background>
                                            <TextBlock  Name="TextBlock" HorizontalAlignment="Center" Text="{Binding PercentOfParent,
                                            Converter={x:Static converters:IntToPercentConverter.Instance}}">
                                            </TextBlock>
                                        </StackPanel>
                                        
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding IsLargeItem}" Value="True">
                                                <Setter TargetName="TextBlock" Property="FontWeight" Value="Bold" />
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>

                            <GridViewColumn Width="120" HeaderContainerStyle="{StaticResource LeftColAlign}"
                                            Header="{Binding Path=LastModifiedColumnHeader}">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Name="TextBlock"  HorizontalAlignment="Left" Text="{Binding LastModified,
                                                Converter={x:Static converters:DateTimeToDateConverter.Instance}}"/>
                                        <DataTemplate.Triggers>
                                            <DataTrigger Binding="{Binding IsLargeItem}" Value="True">
                                                <Setter TargetName="TextBlock" Property="FontWeight" Value="Bold" />
                                            </DataTrigger>
                                        </DataTemplate.Triggers>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView.Columns>
                    </GridView>
                </tree:TreeList.View>
            </tree:TreeList>
            <TextBlock x:Name="InitialText" Margin="10, 30" FontSize="15"
                           Visibility="{Binding ElementName=Tree, Path=HasItems,
                    Converter={x:Static converters:BoolToVisibilityConverter.Instance}}">
                    To analyze your disc space usage, choose a drive or path from the "Scan" menu
            </TextBlock>
        </Grid>
        <StatusBar Grid.Row="2" Background="WhiteSmoke" Height="25">
            <StatusBarItem Width="200" Content="{Binding DiscFreeSpaceInfo}" />
            <Separator />
            <StatusBarItem Width="120" Content="{Binding FilesCountInfo}" />
            <Separator />
            <StatusBarItem Content="{Binding ClusterSizeInfo}" />
        </StatusBar>
    </Grid>
</RibbonWindow>
